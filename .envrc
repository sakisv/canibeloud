[[ .envrc.private ]] && source .envrc.private

export PASSWORD_STORE_DIR=$(pwd)/creds
export HCLOUD_TOKEN=$(pass credentials/hetzner/token)
export CLOUDFLARE_API_TOKEN=$(pass credentials/cloudflare/api_token)
export AWS_ACCESS_KEY_ID=$(pass credentials/aws/access_key_id)
export AWS_SECRET_ACCESS_KEY=$(pass credentials/aws/secret_access_key)
